generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = "***"
}

model User {
  email String  @unique
  id    Int     @default(autoincrement()) @id
  name  String?
} 